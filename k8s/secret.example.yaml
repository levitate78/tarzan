# Template only — create the real secret out-of-band, e.g.:
#   kubectl create secret generic tarzan-secrets \
#     --from-literal=TARZAN_DB_KEY=... \
#     --from-literal=TARZAN_SECRET_KEY=... \
#     --from-literal=TARZAN_ADMIN_USERNAME=... \
#     --from-literal=TARZAN_ADMIN_PASSWORD=...
apiVersion: v1
kind: Secret
metadata:
  name: tarzan-secrets
  labels:
    app: tarzan
type: Opaque
stringData:
  TARZAN_DB_KEY: REPLACE_ME
  TARZAN_SECRET_KEY: REPLACE_ME
  TARZAN_ADMIN_USERNAME: REPLACE_ME
  TARZAN_ADMIN_PASSWORD: REPLACE_ME
